<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imis.agile.module.online.mapper.TableIndexMapper">

    <!-- 在线开发 - 数据库表表索引 通用查询结果列 -->
    <sql id="onl_table_index_base_column_list">
        <!--@sql SELECT -->
        id,
        create_by,
        create_time,
        update_by,
        update_time,
        table_head_id, index_name, index_field, index_type, description, synchronize
        <!--@sql FROM onl_table_index -->
    </sql>

    <!-- 在线开发 - 数据库表表索引 数据表名称 -->
    <sql id="onl_table_index_table_name">
        <!--@sql SELECT TABLE_NAME -->
        onl_table_index
        <!--@sql FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'onl_table_index' -->
    </sql>

</mapper>
